import fs from 'node:fs';
import path from 'node:path';
const root=process.cwd();const read=file=>fs.readFileSync(path.join(root,file),'utf8');const checks=[];const assert=(condition,message)=>{checks.push({condition,message});if(!condition)console.error(`✗ ${message}`);else console.log(`✓ ${message}`);};
const dataContext=read('src/context/DataContext.tsx'),authContext=read('src/context/AuthContext.tsx'),app=read('src/App.tsx'),authModal=read('src/components/auth/AuthModal.tsx'),collections=read('src/components/profile/CollectionsView.tsx'),plans=read('src/components/plan/VybePlanBuilder.tsx'),routeSummary=read('src/components/plan/VybeRouteSummary.tsx'),commandCenter=read('src/components/vybe/VybeCommandCenter.tsx'),dailyDrop=read('src/components/vybe/VybeDailyDrop.tsx'),squadVote=read('src/components/vybe/VybeSquadVote.tsx'),admin=read('src/components/admin/AdminPortal.tsx'),googleMap=read('src/components/map/GoogleMap.tsx'),placeCard=read('src/components/cards/PlaceCard.tsx'),discovery=read('src/services/discoveryService.ts'),googlePlaces=read('src/services/googlePlaces.ts'),googleAdapter=read('src/services/googlePlacesAdapter.ts'),taxonomy=read('src/data/categoryTaxonomy.ts'),savedPlaces=read('src/services/savedPlacesService.ts'),likes=read('src/services/likesService.ts'),plansService=read('src/services/plansService.ts'),collectionsService=read('src/services/collectionsService.ts'),reviewsService=read('src/services/reviewsService.ts'),placesService=read('src/services/placesService.ts'),mappers=read('src/services/mappers.ts'),indexHtml=read('index.html'),serviceWorker=read('public/sw.js'),main=read('src/main.tsx'),envExample=read('.env.example'),ci=read('.github/workflows/ci.yml'),schema=read('supabase/migrations/0001_schema.sql'),googlePersistenceMigration=read('supabase/migrations/0007_google_place_persistence.sql'),googleSecurityMigration=read('supabase/migrations/0009_lock_google_materializer.sql'),categoryMigration=read('supabase/migrations/0010_canonical_category_metadata.sql'),googleServer=read('api/materialize-google-place.ts'),osmProxy=read('api/osm-discovery.ts');
assert(!app.includes('ModeBadge'),'Legacy demo-mode indicator is not part of the shipped app shell.');assert(!authModal.includes('Demo')&&!authModal.includes('demo'),'Authentication UI contains no demo login or persona controls.');assert(!authContext.includes('DEMO_PROFILES')&&!authContext.includes("mode === 'demo'")&&!authContext.includes('enterDemoMode'),'Authentication state contains no demo profile runtime.');assert(!envExample.includes('VITE_DEMO_MODE')&&!envExample.includes('local demo'),'Environment template exposes no demo-mode switch.');assert(!app.includes('<CollectionsView />')||app.includes('privateReady ? <CollectionsView />'),'Saved collections are behind a real authenticated session.');assert(!app.includes('<VybePlanBuilder />')||app.includes('privateReady ? <><VybePlanBuilder />'),'Plans are behind a real authenticated session.');assert(dataContext.includes('getGooglePlaceDetails'),'Public place deep links use the current Google Places details path.');assert(collections.includes('!activeCol.isPublic'),'Private collections cannot accidentally publish broken public links.');assert(collections.includes('await navigator.clipboard.writeText(url)'),'Collection clipboard success is awaited and reported accurately.');assert(plans.includes('if (!requireAuth()) return; setIsCreatingNew(true);'),'Creating a new outing is gated by authentication.');assert(routeSummary.includes('google.com/maps/dir/?api=1'),'Outing route summary exposes a complete Google Maps route.');assert(commandCenter.includes('discoverAtLocation'),'Build My Night can discover from manual city coordinates.');assert(!commandCenter.includes('preview-${index}'),'Build My Night does not fabricate plan item IDs.');assert(dailyDrop.includes('DAILY DROP'),'Daily Drop is wired as a real Explore component.');assert(squadVote.includes('?vote='),'Squad voting creates shareable poll links.');assert(placeCard.includes('Google verified')&&placeCard.includes('VYBE curated'),'Place cards explain their data trust source.');assert(placeCard.includes('getGooglePlaceDetails')&&placeCard.includes('onError'),'Google place photos refresh after an expired image URL fails.');assert(admin.includes('latitude === 0 && longitude === 0'),'Admin creation rejects invalid 0,0 coordinates.');assert(!discovery.includes('unsplash.com'),'Discovery service contains no fake Unsplash place-image fallback.');assert(googlePlaces.includes('Array.isArray(type)')&&googlePlaces.includes('includedTypes')&&!googlePlaces.includes('type.map(placeType => searchNearbyGooglePlacesSingle'),'Google nearby discovery keeps multi-type searches inside one request instead of fanning out quota usage.');assert(discovery.includes('Promise.allSettled')&&discovery.includes('googlePlaces')&&discovery.includes('osmPlaces'),'Discovery merges Google and OpenStreetMap instead of stopping at the first provider.');assert(discovery.includes('function analyzePlace')&&discovery.includes('map(analyzePlace)'),'Every discovered place is classified and analyzed before Explore/Map receive it.');assert(discovery.includes('isGooglePlaceValidForRequest')&&discovery.includes('placeMatchesCanonicalCategory'),'Google discovery rejects results that fail canonical provider validation before Explore receives them.');assert(googleAdapter.includes('isGooglePlaceValidForRequest')&&googleAdapter.includes('providerPlaceId')&&googleAdapter.includes('canonicalCategory')&&googleAdapter.includes('placeMatchesCanonicalCategory'),'Google place validation requires a real Place ID and canonical request-matching category.');assert(taxonomy.includes("games:{")&&taxonomy.includes("googleIncludedTypes:['video_arcade','amusement_center','indoor_playground','bowling_alley"),'Canonical Games category covers current Google game/recreation types.');assert(taxonomy.includes("games:new Set(['restaurant','cafe','bar','night_club','hotel','lodging','shopping_mall','store'])")&&taxonomy.includes('placeMatchesCanonicalCategory'),'Games category rejects obvious restaurant/cafe/bar/hotel/store primary-type contamination.');assert(taxonomy.includes("'salle de jeux'")&&taxonomy.includes("'kids entertainment'"),'Category normalization covers multilingual and kids/game intent aliases.');assert(googlePlaces.includes('useStrictTypeFiltering')&&googlePlaces.includes('includedType'),'Google Text Search uses the current strict type-filtering API option.');assert(googleMap.includes('classifyPlace')&&placeCard.includes('classifyPlace'),'Map markers and cards use the centralized provider-aware classification path.');assert(authContext.includes('toggleLikePlace')&&authContext.includes('likesService.setLiked'),'Like actions update both local UI state and the per-user backend row.');assert(authContext.includes('toggleSavePlace')&&authContext.includes('savedPlacesService.setSaved'),'Save actions update both local UI state and the per-user backend row.');assert(authContext.includes('savedPlacesService.listSavedPlaceIds(userId)')&&authContext.includes('likesService.listLikedPlaceIds(userId)'),'Authenticated users rehydrate persisted backend preferences.');assert(savedPlaces.includes("from('saved_places')")&&savedPlaces.includes('id: newUuid()')&&savedPlaces.includes('ensureGooglePlaceStored(placeId)'),'Saved places have stable row ids and materialize Google places before FK writes.');assert(likes.includes("from('likes')")&&likes.includes('id: newUuid()')&&likes.includes('ensureGooglePlaceStored(placeId)'),'Likes have stable row ids and materialize Google places before FK writes.');assert(plansService.includes("from('plan_items')")&&plansService.includes('plan_id')&&plansService.includes('ensureGooglePlaceStored(item.placeId)'),'Plan items persist against the owning plan and materialize external Google places.');assert(collectionsService.includes("from('collection_items')")&&collectionsService.includes('id: newUuid()')&&collectionsService.includes('ensureGooglePlaceStored(placeId)'),'Collection items have stable row ids and materialize external Google places.');assert(reviewsService.includes("from('reviews')")&&reviewsService.includes('ensureGooglePlaceStored(input.placeId)'),'Reviews materialize external Google places before FK writes.');assert(placesService.includes('ensureGooglePlaceStored'),'Google place materialization remains behind the centralized places service.');assert(mappers.includes('globalThis.crypto')&&mappers.includes('randomUUID')&&mappers.includes('getRandomValues')&&mappers.includes("return'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'"),'UUID fallback remains valid for Supabase UUID columns.');assert(schema.includes('collection_id uuid not null references public.collections(id)'),'Supabase collection item FK uses the same UUID type as collections.id.');assert(googlePersistenceMigration.includes('create or replace function public.ensure_google_place(payload jsonb)')&&googlePersistenceMigration.includes('security definer'),'Legacy Google persistence migration remains present for database history.');assert(googleSecurityMigration.includes('revoke all on function public.ensure_google_place(jsonb) from authenticated'),'Legacy client-callable Google materializer is disabled for authenticated clients.');assert(categoryMigration.includes('canonical_category')&&categoryMigration.includes('provider_primary_type')&&categoryMigration.includes("provider in ('vybe', 'google', 'osm')"),'Canonical category metadata migration supports persisted provider-aware classification.');assert(googleServer.includes('SUPABASE_SERVICE_ROLE_KEY')&&googleServer.includes('GOOGLE_PLACES_SERVER_API_KEY')&&googleServer.includes('verifiedId!==placeId'),'Google place writes require a verified Supabase session and server-side Google identity.');assert(osmProxy.includes('ALLOWED_STATIC_CLAUSES')&&osmProxy.includes('ALLOWED_BROAD_CLAUSES')&&osmProxy.includes('isSafeNameClause')&&osmProxy.includes('MAX_REQUESTS_PER_WINDOW'),'OSM proxy accepts only constrained queries and rate-limits callers.');assert(discovery.includes("fetch('/api/osm-discovery'"),'OSM discovery uses the same-origin server proxy instead of browser-direct Overpass requests.');assert(!discovery.includes('https://overpass-api.de/api/interpreter'),'Browser bundle contains no direct Overpass endpoint.');assert(!googlePlaces.includes('place_of_worship'),'Google Nearby Search does not pass unsupported generic place_of_worship as an included type.');assert(googlePlaces.includes('importLibrary'),'Google Maps uses the current JavaScript API loading path.');assert(indexHtml.includes('<link rel="manifest" href="/manifest.webmanifest" />'),'PWA manifest is linked from the document head.');assert(main.includes("navigator.serviceWorker.register('/sw.js')"),'Production registers the offline shell service worker.');assert(serviceWorker.includes('caches.match(request)'),'Offline shell falls back to cached same-origin content.');assert(ci.includes('npm run typecheck')&&ci.includes('npm run build')&&ci.includes('npm run category-contract'),'CI keeps typecheck, category contract and production build gates enabled.');assert(discovery.includes('discoveryCache')&&discovery.includes('DISCOVERY_CACHE_MS'),'Discovery has a short request deduplication cache.');assert(discovery.includes('Google Places quota is currently exhausted'),'Google quota failures have a safe user-facing fallback message.');
const failed=checks.filter(({condition})=>!condition).length;if(failed){console.error(`\n${failed} production invariant(s) failed.`);process.exit(1);}console.log(`\n${checks.length} production invariants passed.`);
