import { Place, CategoryType, MoodType, PriceLevel, CompanionType, PlaceOpeningHours, ProviderType, PlacePhotoAttribution, PlacePhotoIdentity, VybeCategory } from '../types';
import { GooglePlaceResult } from './googlePlacesTypes';
import { VYBE_CATEGORY_DEFINITIONS, LEGACY_CATEGORY_TO_CANONICAL, normalizeCategoryQuery, classifyProviderPlace, placeMatchesCanonicalCategory, evaluatePlaceRelevance, isGooglePhotoIdentityExact, type ProviderCategoryDefinition } from '../data/categoryTaxonomy';

function googlePriceLevelToVybe(priceLevel?:number):PriceLevel{if(priceLevel===undefined||priceLevel===null)return'$$';const levels:PriceLevel[]=['free','$','$$','$$$','$$$$'];return levels[Math.min(Math.max(priceLevel,0),4)]??'$$';}
function googleApproxCost(priceLevel?:number,isFree=false):number{if(isFree||priceLevel===0)return 0;return[0,10,25,50,100][Math.min(Math.max(priceLevel??2,1),4)]??25;}
function googlePhotoIdentities(photos:GooglePlaceResult['photos'],providerPlaceId:string):PlacePhotoIdentity[]{if(!providerPlaceId)return[];return(photos??[]).flatMap(photo=>{const uri=photo.photo_reference?.trim(),name=photo.name?.trim();if(!uri||!name||!/^https?:\/\//i.test(uri)||!isGooglePhotoIdentityExact(providerPlaceId,name))return[];const attributions:(PlacePhotoAttribution[])=[];for(const author of photo.author_attributions??[]){const displayName=author.displayName?.trim();if(displayName&&!attributions.some(a=>a.displayName===displayName&&a.uri===author.uri))attributions.push({displayName,...(author.uri?{uri:author.uri}:{})});}return[{provider:'google',providerPlaceId,photoName:name,uri,attributions}]});}
function googlePhotoAttributions(identities:PlacePhotoIdentity[]):PlacePhotoAttribution[]{const out:PlacePhotoAttribution[]=[];for(const id of identities)for(const a of id.attributions)if(!out.some(x=>x.displayName===a.displayName&&x.uri===a.uri))out.push(a);return out.slice(0,3);}
function googleWeekdayToOpeningHours(weekdayText?:string[]):Partial<PlaceOpeningHours>{if(!weekdayText?.length)return{};const days=['monday','tuesday','wednesday','thursday','friday','saturday','sunday'];const result:Partial<PlaceOpeningHours>={};for(const entry of weekdayText){const match=entry.match(/^(\w+):\s*(.+)$/);if(!match)continue;const index=days.indexOf(match[1].toLowerCase());if(index>=0)(result as Record<string,string>)[days[index]]=match[2];}return result;}
function hasLateNightHours(weekdayText?:string[]):boolean{return(weekdayText??[]).some(entry=>{const value=entry.toLowerCase();return/24\s*hours|open\s*24/.test(value)||/(?:1|2|3|4|5)(?::\d{2})?\s*(?:am|a\.m\.)\b/.test(value);});}
export function classifyPlace(types?:string[],name?:string):{category:CategoryType;mood:MoodType}{const result=classifyProviderPlace(types??[],undefined,name);return{category:result.legacyCategory,mood:result.mood};}
function googleTypesToSuitableFor(types?:string[]):CompanionType[]{const typeSet=new Set(types??[]);const suitable:CompanionType[]=['solo','friends'];if(['restaurant','cafe','bar','night_club','karaoke','live_music_venue','video_arcade','bowling_alley','amusement_center'].some(t=>typeSet.has(t)))suitable.push('couple','group');if(['park','playground','indoor_playground','amusement_center','amusement_park','shopping_mall','zoo','aquarium','water_park','sports_complex','swimming_pool','children_camp'].some(t=>typeSet.has(t)))suitable.push('family','group');return[...new Set(suitable)];}
export function isGooglePlaceValidForRequest(place:Place,request?:{query?:string;categories?:CategoryType[]}):boolean{if(place.provider!=='google'||!place.providerPlaceId||!place.name.trim())return false;if(!Number.isFinite(place.location.lat)||!Number.isFinite(place.location.lng)||(place.location.lat===0&&place.location.lng===0))return false;const canonicalFromQuery=normalizeCategoryQuery(request?.query),categoryTargets=canonicalFromQuery?[canonicalFromQuery]:(request?.categories??[]).flatMap(category=>LEGACY_CATEGORY_TO_CANONICAL[category]??[]);const requestedCategory=categoryTargets.length===1?categoryTargets[0]:null;const evidence=evaluatePlaceRelevance({provider:'google',providerPlaceId:place.providerPlaceId,providerTypes:place.providerTypes,providerPrimaryType:place.providerPrimaryType,canonicalCategory:place.canonicalCategory,name:place.name,query:request?.query,requestedCategory,distanceKm:place.distanceKm});return evidence.decision==='ACCEPT'||(!categoryTargets.length&&evidence.identityValid&&evidence.providerEvidenceSufficient);}
export function googlePlaceToVybePlace(gp:GooglePlaceResult):Place{const providerPlaceId=gp.place_id?.trim();const providerTypes=[...new Set((gp.types??[]).filter(Boolean))];const analysis=classifyProviderPlace(providerTypes,gp.primary_type,gp.name);const categoryDefinition:ProviderCategoryDefinition=VYBE_CATEGORY_DEFINITIONS[analysis.canonicalCategory];const photoIdentities=googlePhotoIdentities(gp.photos,providerPlaceId||'');const photoAttributions=googlePhotoAttributions(photoIdentities);const openingHours=googleWeekdayToOpeningHours(gp.opening_hours?.weekday_text);const lat=gp.geometry?.location?.lat,lng=gp.geometry?.location?.lng,isFree=gp.price_level===0||providerTypes.some(type=>['park','playground','library','place_of_worship'].includes(type)),canonicalCategory:VybeCategory=analysis.canonicalCategory;const place:Place={id:providerPlaceId?`google:${providerPlaceId}`:`google:${encodeURIComponent(gp.name||'place')}`,provider:'google' as ProviderType,providerPlaceId,providerTypes,providerPrimaryType:gp.primary_type,canonicalCategory,secondaryCategories:analysis.secondaryCategories,providerIdentityConfidence:analysis.confidence,name:gp.name,tagline:gp.vicinity||gp.formatted_address||'',description:'',category:categoryDefinition.legacyCategory,primaryMood:analysis.mood,secondaryMoods:[],location:{address:gp.formatted_address||gp.vicinity||'',neighborhood:'',city:'',lat:Number.isFinite(lat)?lat!:0,lng:Number.isFinite(lng)?lng!:0},priceLevel:isFree?'free':googlePriceLevelToVybe(gp.price_level),approxCostUsd:googleApproxCost(gp.price_level,isFree),rating:gp.rating??0,reviewCount:gp.user_ratings_total??0,baseVybeScore:Math.max(1,Math.min(99,Math.round(70+analysis.confidence*15+Math.min(10,(gp.rating??0)*2)))),images:photoIdentities.map(p=>p.uri),photoAttributions,tags:providerTypes.slice(0,15),estimatedDuration:'',openingHours:{monday:'',tuesday:'',wednesday:'',thursday:'',friday:'',saturday:'',sunday:'',isOpenNow:gp.opening_hours?.open_now,...openingHours},features:{isFree,isOutdoor:providerTypes.some(t=>['park','city_park','state_park','national_park','hiking_area','beach','garden','botanical_garden','campground','zoo','aquarium','wildlife_park','wildlife_refuge'].includes(t)),isIndoor:providerTypes.some(t=>['restaurant','cafe','museum','art_gallery','art_museum','library','movie_theater','video_arcade','amusement_center','indoor_playground','gym','spa','shopping_mall','store'].includes(t)),hasFood:providerTypes.some(t=>['restaurant','cafe','bakery','meal_takeaway','meal_delivery','bar','food_court'].includes(t)),hasAlcohol:providerTypes.some(t=>['bar','night_club','cocktail_bar','brewery','wine_bar'].includes(t)),isLateNight:hasLateNightHours(gp.opening_hours?.weekday_text),isSecretGem:canonicalCategory==='tourist'||canonicalCategory==='entertainment',isPetFriendly:false,isWifiFriendly:providerTypes.includes('internet_cafe'),isPhotoSpot:canonicalCategory==='tourist'||canonicalCategory==='outdoors',isAccessible:false},suitableFor:googleTypesToSuitableFor(providerTypes),website:gp.website,phone:gp.formatted_phone_number,instagram:undefined,isFeatured:false,isTrending:false,reviews:[]};const evidence=evaluatePlaceRelevance({provider:'google',providerPlaceId,providerTypes,providerPrimaryType:gp.primary_type,canonicalCategory,name:gp.name});place.relevance=evidence;return place;}
