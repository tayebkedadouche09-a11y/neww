import { Component, type ErrorInfo, type ReactNode } from 'react';
type Props={children:ReactNode};type State={failed:boolean};
export default class ErrorBoundary extends Component<Props,State>{state:State={failed:false};static getDerivedStateFromError():State{return{failed:true};}componentDidCatch(error:Error,info:ErrorInfo){console.error('VYBE render error',error,info);}render(){if(!this.state.failed)return this.props.children;return <main style={{minHeight:'100vh',display:'grid',placeItems:'center',padding:24,background:'#08090b',color:'#fff',fontFamily:'system-ui'}}><section style={{maxWidth:560,textAlign:'center'}}><div style={{fontSize:48}}>⚠️</div><h1>VYBE a rencontré un problème</h1><p style={{color:'#9ca0a8',lineHeight:1.6}}>Recharge la page pour relancer l’application. Tes favoris, likes et plans sauvegardés localement restent sur cet appareil.</p><button style={{padding:'12px 16px',border:0,borderRadius:12,cursor:'pointer',fontWeight:700}} onClick={()=>location.reload()}>Recharger VYBE</button></section></main>;}}